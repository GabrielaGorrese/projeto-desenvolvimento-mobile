# Copie para .env na raiz do projeto antes de subir com Docker:
#   cp .env.docker.example .env

PORT=3000

DB_NAME=comandou
DB_USER=postgres
DB_PASSWORD=postgres
DB_PORT=5432
DB_POOL_MAX=20

JWT_SECRET=troque_por_uma_string_longa_e_aleatoria
JWT_EXPIRES_IN=8h

TZ=America/Sao_Paulo
